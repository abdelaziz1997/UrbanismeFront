import { CreationDossierForm } from './creation-dossier-form';

describe('CreationDossierForm', () => {
  it('should create an instance', () => {
    expect(new CreationDossierForm()).toBeTruthy();
  });
});
